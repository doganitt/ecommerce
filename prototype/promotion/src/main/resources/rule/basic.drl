import doganitt.ecommerce.prototype.product.ProductEntity;

rule "basic promotion1"
    when
        $product : ProductEntity ( )
    then
       	$product.setDiscountedPrice($product.getSellingPrice()-1000);
end